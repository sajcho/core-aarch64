# Description:	C library for the arithmetic of complex numbers 
# URL:		http://www.multiprecision.org/
# Maintainer:	CRUX System Team, core-ports at crux dot nu
# Depends on:	libmpfr
# Modified:	Milan Buska, milan dot buska at gmail dot com


name=libmpc
version=1.2.0
release=1
source=(https://ftpmirror.gnu.org/gnu/mpc/mpc-$version.tar.gz)

build() {
	cd mpc-$version

	./configure \
		--prefix=/usr \
		--enable-thread-safe

	make
	make DESTDIR=$PKG install

	rm -r $PKG/usr/share
}
