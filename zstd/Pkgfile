# Description:	Real-time compression with high compression ratios.
# URL:		http://www.zstd.net/
# Maintainer:	sajcho, saux dot aarch64 at gmail dot com
# Depends on:	zlib xz

name=zstd
version=1.4.5
release=0
source=(https://github.com/facebook/zstd/archive/v${version}/${name}-v${version}.tar.gz)

build() {
	cd zstd-${version}

	make HAVE_THREAD=1 HAVE_ZLIB=0 HAVE_LZMA=0 HAVE_LZ4=0

	make -C contrib/pzstd

	PREFIX=/usr make DESTDIR=$PKG install

	install -D -m 0755 contrib/pzstd/pzstd $PKG/usr/bin/pzstd
}
