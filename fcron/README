
# NOTE:
#
# After installing fcron, generate a systab file in the directory
# /var/spool/fcron use:

fcrontab -z -u systab

# this will convert systab.orig to systab
# before activating the fcron service

ln -s /etc/sv/fcron /var/service/

#################################################################
